{
  "name": "exercises",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "manifest": "node tools/check-manifest.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "attempt": "EXERCISE_TARGET=starter node --test",
    "verify": "pnpm run manifest && pnpm run typecheck && pnpm run test"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "~6.0.2"
  }
}
